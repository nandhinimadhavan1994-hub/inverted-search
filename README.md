# inverted-search
Developed an Inverted Search system in C that indexes words from multiple text files using a hash table and linked lists. The program allows users to create, search, display, and update the database to efficiently find words across files.
